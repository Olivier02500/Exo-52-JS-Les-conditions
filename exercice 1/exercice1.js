/* Modifier le code ci dessous pour réaliser l'exercice */
let x = 100;
console.log(x)

let y =150;
console.log(y)

if (x > y)
    document.getElementById('monDiv').innerHTML = "La variable x est supérieure à la variable y";

else
    document.getElementById('monDiv').innerHTML = "La variable x est infèrieur à la variable y";
